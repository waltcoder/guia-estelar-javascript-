// Operador de String or (String Operator);

// compareção (comparasion)
console.log('a' == 'b');


// concatenação (concatenation)
// Retorna a união de duas strings
let alpha = 'alpha';
console.log(alpha + 'bet')
// or console.log(alpha +='bet');