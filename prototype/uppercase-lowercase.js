// Manipulando String e Números

// Transformar letras minúsculas em maiúsculas. Faça o contrário disso também
let word = 'Programar é muito bacana';
console.log(word.toUpperCase()); // transformar todas as letras em maiúsculas
console.log(word.toLowerCase()); // transformar todas as letras em minúsculas