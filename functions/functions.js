// função anonima
// function expression or function anonymous

// parâmetros da função or (parameters)
const sum = function (number1, number2, number3) {
  console.log(number1 + number2 + number3 );
}

// run function
sum(2, 3, 9); // argumentos or arguments