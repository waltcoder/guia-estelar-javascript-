// Object
const person = {
  name : 'Walt',
  age : 17,
  weight : 50,
  isAdmin : true,
}

// Imprimindo propriedades do objeto person
console.log(person);
console.log(person.name);

console.log(`${person.name} tem ${person.age} anos`);